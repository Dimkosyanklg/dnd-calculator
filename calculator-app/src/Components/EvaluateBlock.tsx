import React from "react";

type Props = {};

export const EvaluateBlock: React.FC<Props> = ({}) => {
    return (
        <div className="container container__evaluate">
            <button className="evaluate">=</button>
        </div>
    );
};
